[build]
  command = "npm ci --include=dev --omit=optional --ignore-scripts && npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--include=dev --omit=optional --ignore-scripts"
  NPM_CONFIG_AUDIT = "false"
  NPM_CONFIG_FUND = "false"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
